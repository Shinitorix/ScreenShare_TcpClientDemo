<Window x:Class="EmemoriesDesktopViewer.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmemoriesDesktopViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Viewbox Grid.Column="1" Grid.Row="2" Margin="30">
            <Button x:Name="btnSee" Content="Show" Padding="10" Click="BtnSee_Click"/>
        </Viewbox>

        <Viewbox Grid.Column="0" Grid.Row="2" Margin="30">
            <Button x:Name="btnShare" Content="Share" Padding="10" Click="BtnShare_Click"/>
        </Viewbox>

    </Grid>
    
   
</Window>
